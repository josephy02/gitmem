{
  "name": "memlog",
  "version": "0.1.0",
  "description": "Git-native, append-only event log with deterministic projections, for agent memory.",
  "type": "module",
  "license": "MIT",
  "engines": { "node": ">=22" },
  "bin": { "memlog": "./dist/cli.js" },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "prepublishOnly": "npm run build && npm test"
  },
  "dependencies": {
    "commander": "^13.0.0",
    "ulid": "^2.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "fast-check": "^3.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
